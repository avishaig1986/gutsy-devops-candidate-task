install docker
start docker
install minikube

minikube start
minikube addons enable ingress

build server docker
build redis docker
load server image to minikube
load redis image to minikube

minikube start -n 2
minikube addons enable ingress
minikube addons enable storage-provisioner-rancher


docker build ./redis --tag redis-db:1.0.0
docker build ./server --tag go-server:1.0.0

minikube image load redis-db:1.0.0
minikube image load go-server:1.0.0

kubectl apply -k k8s

helm repo add fluent https://fluent.github.io/helm-charts
helm upgrade --install fluent-bit fluent/fluent-bit


export REDIS_PASSWORD


https://github.com/fluent/fluentd-kubernetes-daemonset/blob/master/fluentd-daemonset-elasticsearch.yaml

https://devopscube.com/kubernetes-logging-tutorial/#:~:text=If%20a%20pod%20uses%20STDOUT,gets%20stored%20within%20the%20pod.


Kubernetes Pod Log Location
You can find the kubernetes pod logs in the following directories of every worker node.

/var/log/containers: All the container logs are present in a single location.
/var/log/pods/: Under this location, the container logs are organized in separate pod folders. /var/log/pods/<namespace>_<pod_name>_<pod_id>/<container_name>/. Each pod folder contains the individual container folder and its respective log file. Each folder has a naming scheme that followshelm upgrade --install --wait --create-namespace --namespace logging logging-operator oci://ghcr.io/kube-logging/helm-charts/logging-operator


https://github.com/fluent/helm-charts/tree/main/charts/fluent-bit
helm upgrade --install --wait --create-namespace --namespace logging logging-operator oci://ghcr.io/kube-logging/helm-charts/logging-operator --set logging.enabled=true


kubectl apply -k fluent-bit
helm repo add fluent https://fluent.github.io/helm-charts
helm upgrade --install --wait --namespace logging --values fluent-bit/values.yml fluent-bit fluent/fluent-bit


kubectl apply -k k8s-music-albums-app





sudo apt-add-repository ppa:ansible/ansible
sudo apt update
sudo apt install ansible